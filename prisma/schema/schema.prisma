generator client {
  provider      = "prisma-client"
  moduleFormat  = "esm"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

